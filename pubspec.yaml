name: gestion_sport
description: Outil d'administration du tournoi Ovalies — tirage au sort et génération des matchs.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  flutter_pdfview: ^1.3.2
  http: ^1.2.0
  intl: ^0.19.0
  web: ^1.1.0
  supabase_flutter: ^2.3.4

flutter:
  uses-material-design: true
